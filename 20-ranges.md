# Ranges

- A resource representing a range **should** use two separate fields, with prefixes `start_` and `end_`.

### Inclusive or Exclusive Ranges

- Fields representing ranges **should** use inclusive start and exclusive end values.
